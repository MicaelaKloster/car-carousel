import React from 'react';
import CarCarousel from './CarCarousel';  // Asegúrate de que la ruta sea correcta

const Home = () => {
  return (
    <div>
      <CarCarousel />  {/* Renderiza el componente Carrusel */}
    </div>
  );
};

export default Home;
